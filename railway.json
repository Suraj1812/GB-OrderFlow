{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npm run db:migrate && node dist-server/server/index.js",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "on_failure"
  }
}
